import { FormTypes, SubmissionEventTypes, SubmissionTypes } from '@oneblink/types';
/**
 * Examine a submission and its form definition to validate whether a scheduling
 * workflow event needs to run.
 *
 * #### Example
 *
 * ```js
 * const result = schedulingService.checkFormSchedulingEvent(
 *   form,
 *   submission,
 * )
 * ```
 *
 * @param definition
 * @param submission
 * @returns
 */
export declare function checkForSchedulingEvent(definition: FormTypes.Form, submission: SubmissionTypes.S3SubmissionData['submission']): SubmissionEventTypes.SchedulingSubmissionEvent | undefined;
