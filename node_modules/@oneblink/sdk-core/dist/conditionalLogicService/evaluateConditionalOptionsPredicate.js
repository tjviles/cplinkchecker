"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function evaluateConditionalOptionsPredicate({ predicate, predicateElement, predicateValue: value, }) {
    return predicate.optionIds.some((optionId) => {
        var _a;
        const option = (_a = predicateElement.options) === null || _a === void 0 ? void 0 : _a.find((o) => o.id === optionId);
        if (option) {
            if (Array.isArray(value)) {
                return value.some((modelValue) => {
                    return modelValue === option.value;
                });
            }
            else if (predicateElement.type === 'compliance' && value) {
                return option.value === value.value;
            }
            else {
                return option.value === value;
            }
        }
        else {
            return false;
        }
    });
}
exports.default = evaluateConditionalOptionsPredicate;
//# sourceMappingURL=evaluateConditionalOptionsPredicate.js.map