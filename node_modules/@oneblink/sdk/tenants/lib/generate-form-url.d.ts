export default function generateFormUrl({ formId, token, externalId, preFillFormDataId, endpoint, userToken, previousFormSubmissionApprovalId, }: {
    endpoint: string;
    formId: number;
    token: string;
    externalId?: unknown;
    preFillFormDataId?: string;
    userToken?: string;
    previousFormSubmissionApprovalId?: string;
}): string;
