declare function encryptUserToken({ username, secret, }: {
    username: string;
    secret: string;
}): string;
declare function decryptUserToken({ userToken, secret, }: {
    userToken: string;
    secret: string;
}): string;
export { encryptUserToken, decryptUserToken };
