"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const querystring_1 = __importDefault(require("querystring"));
function generateFormUrl({ formId, token, externalId, preFillFormDataId, endpoint, userToken, previousFormSubmissionApprovalId, }) {
    if (!formId) {
        throw new Error('no form id provided, please provide a form id to generate a url');
    }
    let searchParams = {
        access_key: token,
    };
    if (externalId) {
        searchParams = Object.assign({}, searchParams, { externalId });
    }
    if (preFillFormDataId) {
        searchParams = Object.assign({}, searchParams, { preFillFormDataId });
    }
    if (userToken) {
        searchParams = Object.assign({}, searchParams, { userToken });
    }
    if (previousFormSubmissionApprovalId) {
        searchParams = Object.assign({}, searchParams, {
            previousFormSubmissionApprovalId,
        });
    }
    const search = querystring_1.default.stringify(searchParams);
    return `${endpoint}/${formId}?${search}`;
}
exports.default = generateFormUrl;
//# sourceMappingURL=generate-form-url.js.map